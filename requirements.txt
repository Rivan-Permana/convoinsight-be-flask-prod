# --- core web ---
gunicorn==21.2.0
Flask==3.0.3
Flask-Cors==4.0.0
python-dotenv==1.0.1

# --- data & LLM ---
polars==1.21.0
pandasai>=3.0.0b2
pandasai-litellm==0.0.1
litellm>=1.61.20,<2.0.0

# --- viz (dipakai agent visualizer) ---
plotly==6.3.0

# --- ops/logging (opsional tapi berguna) ---
python-json-logger==3.3.0

# --- gcp clients (wajib utk GCS & Firestore) ---
google-cloud-storage>=2.16.0
google-cloud-firestore>=2.16.0
google-api-core[grpc]>=2.17.0

# --- export to pdf feature ---
reportlab>=3.6

# --- validate api and it's security
requests==2.32.3
cryptography==43.0.3

# --- PostgreSQL (Supabase) ---
SQLAlchemy>=2.0.31,<3.0     # untuk create_engine, NullPool, dll.
psycopg2-binary==2.9.9      # driver PG untuk SQLAlchemy (mudah dipakai di container)

# (opsional, untuk pl.read_database langsung tanpa fallback pandas)
connectorx>=0.3.3
# atau via ADBC:
adbc-driver-postgresql>=1.0.0
supabase>=2,<3
pdfplumber==0.11.8
python-docx==1.2.0
